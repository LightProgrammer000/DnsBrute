# 🔍 DNSBrute - Brute Force de Subdomínios

Este script em Python realiza **força bruta de subdomínios** usando uma wordlist e tenta resolver os subdomínios via DNS.

## 🚀 Como usar

```bash
python3 dnsbrute.py dominio.com wordlist.txt
